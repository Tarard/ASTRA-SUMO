interface:
  display_name: "ASTRA Build"
  short_description: "Build and review SUMO road networks and traffic infrastructure."
  default_prompt: "Use $astra-build to construct or review this SUMO network from the available road, map, signal, and source evidence."
policy:
  allow_implicit_invocation: true
