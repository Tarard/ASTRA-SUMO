interface:
  display_name: "ASTRA Report"
  short_description: "Explain and report completed SUMO traffic experiments."
  default_prompt: "Use $astra-report to explain or write up these completed SUMO results, verify comparison fairness, and keep the claim inside the evidence boundary."
policy:
  allow_implicit_invocation: true
